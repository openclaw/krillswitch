# Local-only bindings for `wrangler dev`. Never deployed; production gets its
# vars/secrets at the go-live checkpoint. No real secrets in this file.
DEV_AUTH_ENABLED=1
BETTER_AUTH_SECRET=krillswitch-local-dev-secret
BETTER_AUTH_URL=http://localhost:8799
